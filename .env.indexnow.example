# Generate different keys for each host. IndexNow keys are public verification tokens
# that must be served from each host as <key>.txt; do not reuse your API keys.
INDEXNOW_MAIN_KEY="replace-with-a-unique-8-to-128-character-key"
INDEXNOW_PAGES_KEY="replace-with-a-unique-8-to-128-character-key"
